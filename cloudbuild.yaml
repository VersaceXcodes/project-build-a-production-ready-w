steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/launchpulse-ai/project-build-a-production-ready-w:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/launchpulse-ai/project-build-a-production-ready-w:latest'
      - '.'

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/launchpulse-ai/project-build-a-production-ready-w:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/launchpulse-ai/project-build-a-production-ready-w:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'project-build-a-production-ready-w'
      - '--image'
      - 'gcr.io/launchpulse-ai/project-build-a-production-ready-w:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'

images:
  - 'gcr.io/launchpulse-ai/project-build-a-production-ready-w:$COMMIT_SHA'
  - 'gcr.io/launchpulse-ai/project-build-a-production-ready-w:latest'

options:
  logging: CLOUD_LOGGING_ONLY
