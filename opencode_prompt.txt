Add the brand reveal sequence on top of the ink animation.

Sequence:

During the first part of the splash (0–1.5s), only show the ink animation (no text).

After ink “settles” (around 1.2–1.8s), reveal the SultanStamp logo and brand name:

Logo appears first with a subtle scale/opacity pop.

Then the word “SultanStamp” (or “Sultans” if that’s what the design uses) reveals with a clean wipe/mask from left to right.

Make it match the screenshot vibe: premium, high-contrast (white text on black area).

Hold the logo for ~0.6–1.0s.

Fade the entire overlay out smoothly (opacity to 0), then unmount and show the landing page.

Notes:

Use the existing logo asset from the project (don’t replace it). If the logo path is unknown, wire it to whatever the navbar uses.

Ensure the overlay unmount triggers sessionStorage flag.

Clicking/tapping skips instantly to unmount.

Deliverables:

A polished intro: ink splash → logo/name → fade out → homepage visible and interactive.

When the user mentions generic pages, map them to concrete files/components by scanning for headings and routes. Examples you MUST follow:
- If the user says "login" or "sign in", search for components containing the literal heading "Sign in to your account" and edit that file.
- If the user says "sign up" or "register", search for "Create your account" or equivalent.
- Always return an explicit list of changed file paths. If your first attempt doesn't change the UI, re-search by heading text and update the correct file.

⚠️ PRIORITY INSTRUCTIONS:
- The user's request above is the PRIMARY task. Complete it first.
- Ignore any existing errors, warnings, or issues you detect in the project unless they directly prevent you from completing the user's requested task.
- Do NOT automatically fix unrelated errors or refactor code unless explicitly requested.
- Focus solely on implementing the user's request: "Add the brand reveal sequence on top of the ink animation.

Sequence:

During the first part of the splash (0–1.5s), only show the ink animation (no text).

After ink “settles” (around 1.2–1.8s), reveal the SultanStamp logo and brand name:

Logo appears first with a subtle scale/opacity pop.

Then the word “SultanStamp” (or “Sultans” if that’s what the design uses) reveals with a clean wipe/mask from left to right.

Make it match the screenshot vibe: premium, high-contrast (white text on black area).

Hold the logo for ~0.6–1.0s.

Fade the entire overlay out smoothly (opacity to 0), then unmount and show the landing page.

Notes:

Use the existing logo asset from the project (don’t replace it). If the logo path is unknown, wire it to whatever the navbar uses.

Ensure the overlay unmount triggers sessionStorage flag.

Clicking/tapping skips instantly to unmount.

Deliverables:

A polished intro: ink splash → logo/name → fade out → homepage visible and interactive."
- Only address errors if they block the specific functionality requested by the user.