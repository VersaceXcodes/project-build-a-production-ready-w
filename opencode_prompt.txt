Fix ONLY the ink/splash SHAPES of the existing intro animation. Do NOT change timing, logo, layout, routing, backend, or any other UI. Everything stays the same except the black “ink” visuals.

PROBLEM TO FIX:
Right now the “ink” looks like squashed black paper / polygon blobs. I need it to look like REAL LIQUID INK SPLASHES (organic curves, drips, droplets) exactly like my reference screenshots.

NON-NEGOTIABLE SHAPE RULES:
- Do NOT use clip-path polygons, triangles, low-point shapes, or sharp corners.
- Do NOT use smoke/fog, blurry circles, soft blobs, or “cloud” gradients as the main shape.
- The edge MUST be organic: rounded lobes + thin tendrils + scattered droplets (like actual ink splatter).
- Add subtle wet highlights (very light specular shine) so it feels glossy/liquid, not flat paper.

HOW TO IMPLEMENT (choose ONE approach and do it properly):

OPTION A (Preferred): SVG ink assets
- Replace the current ink shape with 2–3 SVG splashes that are hand-crafted using Bezier curves (many control points).
- Each splash must include:
  1) a main blob path (organic, rounded, varying thickness)
  2) 6–15 droplet elements (small circles/teardrops) as separate paths
  3) 2–4 tendril/drip paths extending outward
- Add subtle gradients on the main blob (radial/linear) to create wet highlight (very low intensity).
- Animate these SVG layers (scale/translate/opacity) using the existing timeline.

OPTION B: Canvas / procedural ink
- Generate an irregular blob using smooth noise around a base circle (e.g., simplex/perlin-like), then smooth the outline (Catmull-Rom or Bezier).
- Add a few long tendrils (thin splines) and scattered droplets (small circles/teardrops).
- Render with a slight highlight/shininess effect (subtle lighter streaks on the ink).
- Must look like liquid ink splatter, not paper, not smoke.

DELIVERABLE:
- Keep the exact same intro behavior and animation timing.
- ONLY replace the ink/splash visuals so the shapes match the reference: glossy organic splash with droplets and drips.

When the user mentions generic pages, map them to concrete files/components by scanning for headings and routes. Examples you MUST follow:
- If the user says "login" or "sign in", search for components containing the literal heading "Sign in to your account" and edit that file.
- If the user says "sign up" or "register", search for "Create your account" or equivalent.
- Always return an explicit list of changed file paths. If your first attempt doesn't change the UI, re-search by heading text and update the correct file.

⚠️ PRIORITY INSTRUCTIONS:
- The user's request above is the PRIMARY task. Complete it first.
- Ignore any existing errors, warnings, or issues you detect in the project unless they directly prevent you from completing the user's requested task.
- Do NOT automatically fix unrelated errors or refactor code unless explicitly requested.
- Focus solely on implementing the user's request: "Fix ONLY the ink/splash SHAPES of the existing intro animation. Do NOT change timing, logo, layout, routing, backend, or any other UI. Everything stays the same except the black “ink” visuals.

PROBLEM TO FIX:
Right now the “ink” looks like squashed black paper / polygon blobs. I need it to look like REAL LIQUID INK SPLASHES (organic curves, drips, droplets) exactly like my reference screenshots.

NON-NEGOTIABLE SHAPE RULES:
- Do NOT use clip-path polygons, triangles, low-point shapes, or sharp corners.
- Do NOT use smoke/fog, blurry circles, soft blobs, or “cloud” gradients as the main shape.
- The edge MUST be organic: rounded lobes + thin tendrils + scattered droplets (like actual ink splatter).
- Add subtle wet highlights (very light specular shine) so it feels glossy/liquid, not flat paper.

HOW TO IMPLEMENT (choose ONE approach and do it properly):

OPTION A (Preferred): SVG ink assets
- Replace the current ink shape with 2–3 SVG splashes that are hand-crafted using Bezier curves (many control points).
- Each splash must include:
  1) a main blob path (organic, rounded, varying thickness)
  2) 6–15 droplet elements (small circles/teardrops) as separate paths
  3) 2–4 tendril/drip paths extending outward
- Add subtle gradients on the main blob (radial/linear) to create wet highlight (very low intensity).
- Animate these SVG layers (scale/translate/opacity) using the existing timeline.

OPTION B: Canvas / procedural ink
- Generate an irregular blob using smooth noise around a base circle (e.g., simplex/perlin-like), then smooth the outline (Catmull-Rom or Bezier).
- Add a few long tendrils (thin splines) and scattered droplets (small circles/teardrops).
- Render with a slight highlight/shininess effect (subtle lighter streaks on the ink).
- Must look like liquid ink splatter, not paper, not smoke.

DELIVERABLE:
- Keep the exact same intro behavior and animation timing.
- ONLY replace the ink/splash visuals so the shapes match the reference: glossy organic splash with droplets and drips."
- Only address errors if they block the specific functionality requested by the user.