Update ONLY the branding shown inside the intro overlay. The current logo shown is WRONG (it looks like a generic square/white badge). I need the overlay to display the SAME SultanStamp logo/wordmark used on the website navbar/brand assets.

Requirements:
- Pull the logo asset from the same source the header/navbar uses (reuse the existing import/path; do not introduce a new placeholder logo).
- Keep the logo colors exactly as the real brand (no forced monochrome unless my site brand is monochrome).
- Match my reference layout:
  - After ink slides left, show the SultanStamp logo + “SultanStamp” on the LEFT (on/over the black area).
  - Optionally show “Sultans” (or the right-side brand text I provided) on the RIGHT on the clean white area, using a bold premium font consistent with the site.
- Add a clean reveal: logo pops in (opacity+scale), wordmark wipes in left→right.
- Ensure sizing/position is responsive: looks correct on mobile and desktop.

Fail-safes:
- If logo import fails, fallback to rendering just the text “SultanStamp” (no placeholder icon).
- Do not change any global styles; scope styles to the overlay.

Deliverable:
- The overlay brand matches the actual SultanStamp logo/wordmark used on the site (not a different one).

When the user mentions generic pages, map them to concrete files/components by scanning for headings and routes. Examples you MUST follow:
- If the user says "login" or "sign in", search for components containing the literal heading "Sign in to your account" and edit that file.
- If the user says "sign up" or "register", search for "Create your account" or equivalent.
- Always return an explicit list of changed file paths. If your first attempt doesn't change the UI, re-search by heading text and update the correct file.

⚠️ PRIORITY INSTRUCTIONS:
- The user's request above is the PRIMARY task. Complete it first.
- Ignore any existing errors, warnings, or issues you detect in the project unless they directly prevent you from completing the user's requested task.
- Do NOT automatically fix unrelated errors or refactor code unless explicitly requested.
- Focus solely on implementing the user's request: "Update ONLY the branding shown inside the intro overlay. The current logo shown is WRONG (it looks like a generic square/white badge). I need the overlay to display the SAME SultanStamp logo/wordmark used on the website navbar/brand assets.

Requirements:
- Pull the logo asset from the same source the header/navbar uses (reuse the existing import/path; do not introduce a new placeholder logo).
- Keep the logo colors exactly as the real brand (no forced monochrome unless my site brand is monochrome).
- Match my reference layout:
  - After ink slides left, show the SultanStamp logo + “SultanStamp” on the LEFT (on/over the black area).
  - Optionally show “Sultans” (or the right-side brand text I provided) on the RIGHT on the clean white area, using a bold premium font consistent with the site.
- Add a clean reveal: logo pops in (opacity+scale), wordmark wipes in left→right.
- Ensure sizing/position is responsive: looks correct on mobile and desktop.

Fail-safes:
- If logo import fails, fallback to rendering just the text “SultanStamp” (no placeholder icon).
- Do not change any global styles; scope styles to the overlay.

Deliverable:
- The overlay brand matches the actual SultanStamp logo/wordmark used on the site (not a different one)."
- Only address errors if they block the specific functionality requested by the user.