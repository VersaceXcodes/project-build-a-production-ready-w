Update ONLY the existing intro overlay animation (InkIntroOverlay). Do NOT change backend, routes, data fetching, or page structure.

Problem: current effect looks like smoke/fog because it relies on blurred gradients. I need a true INK SPLASH like my reference screenshots: thick black liquid blobs, sharp edges, splatter droplets, and a glossy wet look.

Change the animation implementation to one of these (choose best/cleanest):
A) SVG-based ink blobs + gooey filter + animated circles
OR
B) Canvas particle blobs with metaballs / simple “goo” merging

Must-have visual constraints:
- Black ink should be opaque, with crisp edges (no large soft haze).
- Splatter droplets: several small drops fling outward and settle.
- Slight glossy highlights on the ink (subtle specular streaks) so it feels wet, not smoke.
- Keep any blur ONLY inside the gooey/metaball merging, not as a whole-screen fog.
- Background should be clean (white or very light) so the black ink reads strongly.

Timing:
- 0.0–1.2s: drops fall + splash expands.
- 1.2–1.8s: ink settles (minor wobble), droplets finish.

Performance:
- 60fps friendly, mobile safe.
- No heavy dependencies added.

Deliverable:
- The overlay now visually matches an ink splat (sharp, liquid) instead of smoke.

When the user mentions generic pages, map them to concrete files/components by scanning for headings and routes. Examples you MUST follow:
- If the user says "login" or "sign in", search for components containing the literal heading "Sign in to your account" and edit that file.
- If the user says "sign up" or "register", search for "Create your account" or equivalent.
- Always return an explicit list of changed file paths. If your first attempt doesn't change the UI, re-search by heading text and update the correct file.

⚠️ PRIORITY INSTRUCTIONS:
- The user's request above is the PRIMARY task. Complete it first.
- Ignore any existing errors, warnings, or issues you detect in the project unless they directly prevent you from completing the user's requested task.
- Do NOT automatically fix unrelated errors or refactor code unless explicitly requested.
- Focus solely on implementing the user's request: "Update ONLY the existing intro overlay animation (InkIntroOverlay). Do NOT change backend, routes, data fetching, or page structure.

Problem: current effect looks like smoke/fog because it relies on blurred gradients. I need a true INK SPLASH like my reference screenshots: thick black liquid blobs, sharp edges, splatter droplets, and a glossy wet look.

Change the animation implementation to one of these (choose best/cleanest):
A) SVG-based ink blobs + gooey filter + animated circles
OR
B) Canvas particle blobs with metaballs / simple “goo” merging

Must-have visual constraints:
- Black ink should be opaque, with crisp edges (no large soft haze).
- Splatter droplets: several small drops fling outward and settle.
- Slight glossy highlights on the ink (subtle specular streaks) so it feels wet, not smoke.
- Keep any blur ONLY inside the gooey/metaball merging, not as a whole-screen fog.
- Background should be clean (white or very light) so the black ink reads strongly.

Timing:
- 0.0–1.2s: drops fall + splash expands.
- 1.2–1.8s: ink settles (minor wobble), droplets finish.

Performance:
- 60fps friendly, mobile safe.
- No heavy dependencies added.

Deliverable:
- The overlay now visually matches an ink splat (sharp, liquid) instead of smoke."
- Only address errors if they block the specific functionality requested by the user.