Change the blob/shape animation to look like realistic black paint splatter instead of smooth blobs. The shape should have:

Radiating tendrils/fingers - Instead of smooth rounded edges, the shape should have irregular, jagged edges with thin paint fingers extending outward from the main body, like an actual paint splash captured mid-explosion
Glossy wet appearance - Add specular highlights and reflections to make the black paint look wet and viscous, with subtle gray/white shine spots that catch light
Satellite droplets - Small scattered paint droplets should break away from the main shape and float independently around the edges
Dripping effects - Some tendrils should have teardrop-shaped drips at their ends, showing gravity's effect on liquid paint
Organic chaotic edges - The boundary between black and white should be irregular and unpredictable, not smooth curves - think actual paint splatter physics
Remove the 3D spheres/bubbles - Replace the floating orbs with flat paint droplets instead

Reference: The effect should look like high-speed photography of black ink or paint hitting a surface and splashing outward.

When the user mentions generic pages, map them to concrete files/components by scanning for headings and routes. Examples you MUST follow:
- If the user says "login" or "sign in", search for components containing the literal heading "Sign in to your account" and edit that file.
- If the user says "sign up" or "register", search for "Create your account" or equivalent.
- Always return an explicit list of changed file paths. If your first attempt doesn't change the UI, re-search by heading text and update the correct file.

⚠️ PRIORITY INSTRUCTIONS:
- The user's request above is the PRIMARY task. Complete it first.
- Ignore any existing errors, warnings, or issues you detect in the project unless they directly prevent you from completing the user's requested task.
- Do NOT automatically fix unrelated errors or refactor code unless explicitly requested.
- Focus solely on implementing the user's request: "Change the blob/shape animation to look like realistic black paint splatter instead of smooth blobs. The shape should have:

Radiating tendrils/fingers - Instead of smooth rounded edges, the shape should have irregular, jagged edges with thin paint fingers extending outward from the main body, like an actual paint splash captured mid-explosion
Glossy wet appearance - Add specular highlights and reflections to make the black paint look wet and viscous, with subtle gray/white shine spots that catch light
Satellite droplets - Small scattered paint droplets should break away from the main shape and float independently around the edges
Dripping effects - Some tendrils should have teardrop-shaped drips at their ends, showing gravity's effect on liquid paint
Organic chaotic edges - The boundary between black and white should be irregular and unpredictable, not smooth curves - think actual paint splatter physics
Remove the 3D spheres/bubbles - Replace the floating orbs with flat paint droplets instead

Reference: The effect should look like high-speed photography of black ink or paint hitting a surface and splashing outward."
- Only address errors if they block the specific functionality requested by the user.